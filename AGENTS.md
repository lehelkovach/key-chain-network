# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository (`key-chain-network`) with no application code yet. The repo currently contains only a `README.md`.

- **No build system, package manager, or dependencies** are configured.
- **No services** need to be started.
- **No tests** exist to run.
- **No lint or formatting** tools are configured.

As the project evolves, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run the dev server, tests, and linter.
- Any non-obvious startup caveats or environment requirements.
